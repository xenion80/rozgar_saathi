spring:
  application:
    name: backend

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: jdbc:postgresql://ep-cool-butterfly-b3p96fxo-pooler.c-4.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    #url: jdbc:postgresql://localhost:5432/AI-Vulnerability-Prioritizer
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

cloudinary:
  url: ${CLOUDINARY_URL:}


jwt:
  secretKey: ${JWT_SECRET}

app:
  base-url: ${BASE_URL}
  frontend-url: ${FRONTEND_URL:http://localhost:3000}

gemini:
  api:
    key: ${GEMINI_API_KEY:}

